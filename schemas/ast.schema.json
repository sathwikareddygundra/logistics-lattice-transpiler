{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://logistics-lattice-transpiler.local/schemas/ast.schema.json",
  "title": "AstDocument",
  "description": "T-15: one JSON Schema covering both the Python-target and SQL-target AST node types (T-63's decision: a shared grammar, distinguished only by the document-level `target` field), feeding both codegen stages (Phases 10 and 11). op values, and each op's params shape, must exactly match ast_.nodes (T-63) -- see tests/test_ast_schema.py::test_op_enum_matches_nodes. This schema covers per-object shape only; graph-level invariants (unique node ids, unique var bindings, edges referencing real nodes, acyclicity, join edge arity) are enforced by schemas/validate_ast.py on top of it, backed by ast_.nodes.AstDocument.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "trace_id", "target", "nodes", "edges"],
  "properties": {
    "schema_version": {
      "description": "Contract version for this document shape. Bump policy is formalized in T-16.",
      "type": "string",
      "const": "1.0.0"
    },
    "trace_id": {
      "description": "The T-6 trace_id this query is carrying end to end.",
      "type": "string",
      "format": "uuid"
    },
    "target": {
      "description": "Which codegen pass consumes this document. A document never mixes targets.",
      "type": "string",
      "enum": ["python", "sql"]
    },
    "nodes": {
      "description": "The typed, named statements that make up this AST (T-64 lowers an IR document into these).",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/node" }
    },
    "edges": {
      "description": "Directed data-flow dependencies between nodes.",
      "type": "array",
      "items": { "$ref": "#/$defs/edge" }
    }
  },
  "$defs": {
    "ir_type": {
      "type": "string",
      "enum": ["integer", "float", "string", "boolean", "date", "datetime"]
    },
    "column_ref": {
      "type": "object",
      "additionalProperties": false,
      "required": ["table", "column", "type"],
      "properties": {
        "table": { "type": "string", "minLength": 1 },
        "column": { "type": "string", "minLength": 1 },
        "type": { "$ref": "#/$defs/ir_type" }
      }
    },
    "literal": {
      "type": "object",
      "additionalProperties": false,
      "required": ["value", "type"],
      "properties": {
        "value": { "type": ["string", "number", "boolean"] },
        "type": { "$ref": "#/$defs/ir_type" }
      }
    },
    "node": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "op", "var", "params"],
      "properties": {
        "id": {
          "description": "Unique within the ast document's nodes array.",
          "type": "string",
          "minLength": 1
        },
        "op": {
          "description": "Reuses T-56's IR op vocabulary unchanged -- see T-63's ADR.",
          "type": "string",
          "enum": [
            "select",
            "filter",
            "join",
            "aggregate",
            "window",
            "sort",
            "limit",
            "forecast",
            "output"
          ]
        },
        "var": {
          "description": "The name this node's result is bound to (a Python variable, or a SQL CTE/subquery alias). Unique within the document.",
          "type": "string",
          "minLength": 1
        },
        "params": {
          "description": "Shape depends on op -- see the if/then branches below. Identical for either target: rendering differences are handled by codegen (T-70/T-76), not this grammar.",
          "type": "object"
        }
      },
      "allOf": [
        {
          "if": { "properties": { "op": { "const": "select" } }, "required": ["op"] },
          "then": { "properties": { "params": { "$ref": "#/$defs/select_params" } } }
        },
        {
          "if": { "properties": { "op": { "const": "filter" } }, "required": ["op"] },
          "then": { "properties": { "params": { "$ref": "#/$defs/filter_params" } } }
        },
        {
          "if": { "properties": { "op": { "const": "join" } }, "required": ["op"] },
          "then": { "properties": { "params": { "$ref": "#/$defs/join_params" } } }
        },
        {
          "if": { "properties": { "op": { "const": "aggregate" } }, "required": ["op"] },
          "then": { "properties": { "params": { "$ref": "#/$defs/aggregate_params" } } }
        },
        {
          "if": { "properties": { "op": { "const": "window" } }, "required": ["op"] },
          "then": { "properties": { "params": { "$ref": "#/$defs/window_params" } } }
        },
        {
          "if": { "properties": { "op": { "const": "sort" } }, "required": ["op"] },
          "then": { "properties": { "params": { "$ref": "#/$defs/sort_params" } } }
        },
        {
          "if": { "properties": { "op": { "const": "limit" } }, "required": ["op"] },
          "then": { "properties": { "params": { "$ref": "#/$defs/limit_params" } } }
        },
        {
          "if": { "properties": { "op": { "const": "forecast" } }, "required": ["op"] },
          "then": { "properties": { "params": { "$ref": "#/$defs/forecast_params" } } }
        },
        {
          "if": { "properties": { "op": { "const": "output" } }, "required": ["op"] },
          "then": { "properties": { "params": { "$ref": "#/$defs/output_params" } } }
        }
      ]
    },
    "edge": {
      "type": "object",
      "additionalProperties": false,
      "required": ["from_node", "to_node"],
      "properties": {
        "from_node": { "type": "string", "minLength": 1 },
        "to_node": { "type": "string", "minLength": 1 },
        "role": {
          "description": "Only meaningful for multi-input nodes (currently: join).",
          "type": "string",
          "enum": ["left", "right"]
        }
      }
    },
    "select_params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["table", "columns"],
      "properties": {
        "table": { "type": "string", "minLength": 1 },
        "columns": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/column_ref" }
        }
      }
    },
    "filter_params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["column", "operator", "value"],
      "properties": {
        "column": { "$ref": "#/$defs/column_ref" },
        "operator": {
          "type": "string",
          "enum": ["eq", "ne", "gt", "gte", "lt", "lte"]
        },
        "value": { "$ref": "#/$defs/literal" }
      }
    },
    "join_params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["left_key", "right_key"],
      "properties": {
        "left_key": { "$ref": "#/$defs/column_ref" },
        "right_key": { "$ref": "#/$defs/column_ref" },
        "how": {
          "type": "string",
          "enum": ["inner", "left", "right", "outer"],
          "default": "inner"
        }
      }
    },
    "aggregate_params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["metric", "op", "result_type"],
      "properties": {
        "group_by": {
          "type": "array",
          "items": { "$ref": "#/$defs/column_ref" },
          "default": []
        },
        "metric": { "$ref": "#/$defs/column_ref" },
        "op": {
          "type": "string",
          "enum": ["sum", "count", "avg", "min", "max"]
        },
        "result_type": { "$ref": "#/$defs/ir_type" }
      }
    },
    "window_params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["column", "granularity"],
      "properties": {
        "column": { "$ref": "#/$defs/column_ref" },
        "granularity": {
          "type": "string",
          "enum": ["day", "week", "month", "quarter", "year"]
        }
      }
    },
    "sort_params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["by"],
      "properties": {
        "by": { "$ref": "#/$defs/column_ref" },
        "direction": {
          "type": "string",
          "enum": ["asc", "desc"],
          "default": "asc"
        }
      }
    },
    "limit_params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["count"],
      "properties": {
        "count": { "type": "integer", "minimum": 1 }
      }
    },
    "forecast_params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["metric", "horizon"],
      "properties": {
        "metric": { "$ref": "#/$defs/column_ref" },
        "horizon": { "type": "integer", "minimum": 1 },
        "method": { "type": "string", "default": "linear" }
      }
    },
    "output_params": {
      "type": "object",
      "additionalProperties": false,
      "required": [],
      "properties": {
        "columns": {
          "type": "array",
          "items": { "$ref": "#/$defs/column_ref" },
          "default": []
        }
      }
    }
  }
}
